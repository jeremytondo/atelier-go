```bash
export ATELIER_HOST=localhost
export ATELIER_REMOTE_PATH=$HOME/tmp/atelier-go/bash/atelier-server 
  ~/tmp/atelier-go/bash/atelier-client launch 
```

(Note: SSH to localhost might require setup keys)
